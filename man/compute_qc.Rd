% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_metrics.R
\name{compute_qc}
\alias{compute_qc}
\title{Compute per-cell QC metrics}
\usage{
compute_qc(
  sce,
  mito_pattern = "^[Mm][Tt]-",
  run_doublets = TRUE,
  seed = 100L,
  doublet_args = list(),
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment}
with a \code{counts} assay.}

\item{mito_pattern}{Regular expression matching mitochondrial gene symbols.
The default \code{"^[Mm][Tt]-"} matches human (\verb{MT-}) and mouse (\verb{mt-}).}

\item{run_doublets}{Logical; run scDblFinder (default \code{TRUE}). Falls back to
\code{NA} doublet metrics if scDblFinder is unavailable or errors.}

\item{seed}{Random seed for the (stochastic) doublet step.}

\item{doublet_args}{Extra arguments passed to \code{\link[scDblFinder:scDblFinder]{scDblFinder::scDblFinder()}}.}

\item{verbose}{Emit a warning when no mitochondrial genes match.}
}
\value{
The input SCE with extra \code{colData} columns (see \code{\link[=qc_metric_cols]{qc_metric_cols()}})
and a \code{metadata(sce)$cellboard$qc} record.
}
\description{
Adds standardised per-cell QC metrics to an SCE: UMIs (\code{nUMI}), genes
detected (\code{nGene}), mitochondrial percentage (\code{pct_mito}) and, optionally,
an \link[scDblFinder:scDblFinder]{scDblFinder} doublet score (\code{doublet_score})
and class (\code{doublet_class}).
}
\details{
Mitochondrial genes are found by matching \code{mito_pattern} against gene symbols
(taken from a \code{Symbol}-like \code{rowData} column when present, otherwise the row
names), so it works whether your features are Ensembl IDs or symbols.
}
\examples{
sce <- example_sce()
sce <- compute_qc(sce, run_doublets = FALSE)
head(as.data.frame(SummarizedExperiment::colData(sce)))
}
