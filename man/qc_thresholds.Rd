% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_metrics.R
\name{qc_thresholds}
\alias{qc_thresholds}
\title{Construct a set of QC thresholds}
\usage{
qc_thresholds(
  min_genes = NULL,
  max_genes = NULL,
  min_umi = NULL,
  max_umi = NULL,
  max_mito = NULL,
  max_doublet = NULL,
  remove_doublet_class = FALSE
)
}
\arguments{
\item{min_genes, max_genes}{Minimum/maximum genes detected (\code{nGene}).}

\item{min_umi, max_umi}{Minimum/maximum UMIs (\code{nUMI}).}

\item{max_mito}{Maximum mitochondrial percentage (\code{pct_mito}).}

\item{max_doublet}{Maximum doublet score (\code{doublet_score}).}

\item{remove_doublet_class}{Logical; also discard cells whose
\code{doublet_class} is \code{"doublet"}.}
}
\value{
An object of class \code{cb_thresholds} (a validated list).
}
\description{
Each threshold is either a single number (active) or \code{NULL} (inactive). A
cell is discarded if it fails \emph{any} active threshold.
}
\examples{
qc_thresholds(min_genes = 200, max_mito = 10, remove_doublet_class = TRUE)
}
