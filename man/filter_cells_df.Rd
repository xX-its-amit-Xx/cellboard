% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_metrics.R
\name{filter_cells_df}
\alias{filter_cells_df}
\title{Decide which cells to keep, from a data.frame of metrics}
\usage{
filter_cells_df(df, thresholds = default_thresholds())
}
\arguments{
\item{df}{A data.frame with metric columns (\code{nUMI}, \code{nGene}, \code{pct_mito},
\code{doublet_score}, \code{doublet_class}).}

\item{thresholds}{A \code{cb_thresholds} object or a named list.}
}
\value{
A list with \code{keep} (logical), \code{discard} (logical) and \code{reasons}
(a data.frame of per-filter logicals; \code{TRUE} = failed that filter).
}
\description{
The pure filtering core. Operates on any data.frame that has the standardised
metric columns (see \code{\link[=qc_metric_cols]{qc_metric_cols()}}); missing columns or \code{NA} values never
cause a cell to be discarded.
}
\examples{
df <- data.frame(nUMI = c(100, 5000), nGene = c(50, 1500), pct_mito = c(30, 5))
filter_cells_df(df, qc_thresholds(min_genes = 200, max_mito = 10))$keep
}
