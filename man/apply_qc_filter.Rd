% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_metrics.R
\name{apply_qc_filter}
\alias{apply_qc_filter}
\title{Apply QC thresholds to a SingleCellExperiment}
\usage{
apply_qc_filter(sce, thresholds = default_thresholds())
}
\arguments{
\item{sce}{An SCE with QC metrics (from \code{\link[=compute_qc]{compute_qc()}}).}

\item{thresholds}{A \code{cb_thresholds} object or named list.}
}
\value{
A list with elements \code{sce} (annotated), \code{filtered} (kept cells only),
\code{keep}, \code{discard}, \code{reasons}, and the counts \code{n_total}, \code{n_keep},
\code{n_discard}.
}
\description{
Annotates the SCE with \code{discard}/\code{qc_keep} columns and returns both the
annotated object and the filtered subset.
}
\examples{
sce <- compute_qc(example_sce(), run_doublets = FALSE)
res <- apply_qc_filter(sce, qc_thresholds(min_genes = 200, max_mito = 15))
res$n_keep
}
